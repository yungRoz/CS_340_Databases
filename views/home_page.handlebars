<form id="giveReview" align="left">
  <fieldset>
    <h2> Give a review! </h2>

    <label>Who is this review for?</label>
    <br/>
    <input type="text" name="name" id="name" />
    <br/>
    <br/>
    <select>
      <optgroup label="Swedish Cars">
        <option value="volvo">Volvo</option>
        <option value="saab">Saab</option>
      </optgroup>
      <optgroup label="German Cars">
        <option value="mercedes">Mercedes</option>
        <option value="audi">Audi</option>
      </optgroup>
    </select>

    <label>email</label>
    <br/>
    <input type="text" name="email" id="email" />
    <br/>
    <br/>

    <input type="radio" name="lbs" id="lbs" value="true" />lbs</label>
    <input type="radio" name="lbs" id="kg" value="false" />kg</label>
    <input type="submit" id="reviewPerson" />
  </fieldset>
</form>

<form id="addToCompany" align="left">
  <fieldset>
    <h2> Add someone to your company! </h2>
    <br/>
    <label>Which company do you want to add them to?</label>
    <br/>
    <select name="company" id="addCompany">
      {{#if results}}
        {{#each results}}
          {{#if this.co_id}}
          <option value={{this.co_id}}> {{this.co_name}}</option>
          {{/if}}
        {{/each}}
      {{/if}}
    </select>
    <br />
    <label>Who do you want to add?</label>
    <br/>
    <select name="person" id="addPerson">
    {{#if results}}
      {{#each results}}
        {{#if this.notu_id}}
        <option value={{this.notu_id}}> {{this.notu_name}}</option>
        {{/if}}
      {{/each}}
    {{/if}}
    </select>
    <input type="submit" id="addPersonToCompany" />
  </fieldset>
</form>



<table name="friends" id="Friends" align="center">
  <caption> Friends </caption>
  <tr class="header">
    <th>Name</th>
    <th>Star Rating</th>
    <th>Top Classifer</th>
    <th>Delete</th>
  </tr>
  {{#if results}}
    {{#each results}}
      {{#if this.fr_name}}
        <tr class="row">
          <td>{{this.fr_name}}</td>
          <td>{{this.fr_rating}}</td>
          <td>{{this.fr_term}}</td>
          <td> <input type="button" name="delete" value="Delete" onClick="deleteCoRow({{this.fr_per_id}}, {{this.fr_co_id}}, 'Friends')"><input type="hidden" id="{{this.fr_per_id}}"></td>
        </tr>
      {{/if}}
      {{#if this.co_id}}
    {{/each}}
  {{/if}}
</table>




<table name="family" id="Family" align="center">
  <caption> Family </caption>
  <tr class="header">
    <th>Name</th>
    <th>Star Rating</th>
    <th>Top Classifer</th>
    <th>Delete</th>
  </tr>
  {{#if results}}
    {{#each results}}
      {{#if this.fa_name}}
        <tr class="row">
          <td>{{this.fa_name}}</td>
          <td>{{this.fa_rating}}</td>
          <td>{{this.fa_term}}</td>
          <td> <input type="button" name="delete" value="Delete" onClick="deleteCoRow({{this.fa_per_id}}, {{this.fa_co_id}}, 'Family')"><input type="hidden" id="{{this.fa_per_id}}"></td>
        </tr>
      {{/if}}
    {{/each}}
  {{/if}}
</table>


<table name="work" id="Work" align="center">
  <caption> Work </caption>
  <tr class="header">
    <th>Name</th>
    <th>Star Rating</th>
    <th>Top Classifer</th>
    <th>Delete</th>
  </tr>
  {{#if results}}
    {{#each results}}
      {{#if this.w_name}}
        <tr class="row">
          <td>{{this.w_name}}</td>
          <td>{{this.w_rating}}</td>
          <td>{{this.w_term}}</td>
          <td> <input type="button" name="delete" value="Delete" onClick="deleteRow({{this.w_per_id}},{{this.w_co_id}}, 'Work')"><input type="hidden" id="{{this.w_per_id}}"></td>
        </tr>
      {{/if}}
    {{/each}}
  {{/if}}
</table>

<script type="text/javascript" language="javascript" src="hp_script.js">
</script>
